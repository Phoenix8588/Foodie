import { RestfilterPipe } from './restfilter.pipe';

describe('RestfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RestfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
